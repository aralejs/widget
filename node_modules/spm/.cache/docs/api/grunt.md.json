{"title":"grunt","meta":{"title":"grunt","pubdate":"2013-03-26","index":"10","description":"<p>Hack the grunt.</p>","filepath":"docs/api/grunt.md","filename":"grunt","directory":"api","id":"api-grunt"},"body":"\nspm hacks in grunt, extends grunt methods.\n\n```js\nvar grunt = require('spm').sdk.grunt;\n```\n\nIt contains everything of grunt, and more.\n\n## invokeTask(name, options)\n\nInvoke/call a task.\n\n```js\ngrunt.invokeTask('build', {\n  fallback: function(grunt) {\n      console.log('fallback')\n  }\n})\n```\n\nIt will search the gruntfiles and detect if the task name in the gruntfile. If the task is in the gruntfile, it will run the task, if not, it will call the fallback function.\n\n## loadGlobalTasks(name)\n\nLoad tasks in the global `NODE_PATH`. Just like `loadNpmTasks`, it can load task collections.","abspath":"/Users/lepture/workspace/node/spm/docs/api/grunt.md","filepath":"docs/api/grunt.md","filename":"grunt","directory":"api","id":"api-grunt","tags":[],"pubdate":"2013-03-26T00:00:00.000Z","status":"public","html":"<p>spm hacks in grunt, extends grunt methods.</p>\n<div class=\"highlight\"><pre><code class=\"javascript\"><span class=\"keyword\">var</span> grunt = require(<span class=\"string\">'spm'</span>).sdk.grunt;</code></pre></div><p>It contains everything of grunt, and more.</p>\n<h2 id=\"invoketask-name-options\">invokeTask(name, options)</h2><p>Invoke/call a task.</p>\n<div class=\"highlight\"><pre><code class=\"javascript\">grunt.invokeTask(<span class=\"string\">'build'</span>, {\n  fallback: <span class=\"keyword\">function</span>(grunt) {\n      console.log(<span class=\"string\">'fallback'</span>)\n  }\n})</code></pre></div><p>It will search the gruntfiles and detect if the task name in the gruntfile. If the task is in the gruntfile, it will run the task, if not, it will call the fallback function.</p>\n<h2 id=\"loadglobaltasks-name\">loadGlobalTasks(name)</h2><p>Load tasks in the global <code>NODE_PATH</code>. Just like <code>loadNpmTasks</code>, it can load task collections.</p>\n","toc":"<ul><li><a href=\"#invoketask-name-options\">invokeTask(name, options)</a></li><li><a href=\"#loadglobaltasks-name\">loadGlobalTasks(name)</a></li></ul>","iframes":{}}