#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../win-spawn/bin/win-line-endings" "$@"
  ret=$?
else 
  node  "$basedir/../win-spawn/bin/win-line-endings" "$@"
  ret=$?
fi
exit $ret
